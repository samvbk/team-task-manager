[build]
builder = "nixpacks"

[deploy]
startCommand = "npx prisma db push --accept-data-loss && npm run db:seed && node src/index.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
